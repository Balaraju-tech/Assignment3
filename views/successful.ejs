<!DOCTYPE html>
<html lang="en">
<head>
  <title>Add new Data</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.0/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/js/bootstrap.min.js"></script>
  <!-- For DataTables -->
  <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.16/css/jquery.dataTables.css">
  <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.10.16/js/jquery.dataTables.js"></script>
</head>
<body>

<h1>your movies are submitted</h1>
<input type="text" id="searchbyuniquemovie" placeholder="Search By Movie name">
<button type="button" id="searchmov" class="btn btn-success" href="/searchbymovie">Search By moviename</button>
<br>
<br>
<a href="/">Click here to create a movie list</a>
<a href="/getachievementmovies"><button>Get All achievement movies</button></a>

<a href="/getallmovies"><button id="getall">Get All Movies</button></a>
<a href="/getthreetoprated"><button id="getthreetoprated">Get three top rated movies</button></a>


<table class="table table-striped table-inverse table-responsive">
    <thead class="thead-inverse">
        <tr>
            <th>Movies</th>
            <th>Genres</th>
            <th>Rating</th>
            <th>Language</th>
        </tr>
        </thead>
        <tbody id="tablebody">
            <% if(data=="" || data== "no data" || data=="No Data Saved"){%>
                <tr>No Data Available/ No Data Saved (Click Get Movies to get the Movies)</tr> 
            <% }else{ %>
                <% data.forEach(function(singledata){%>
                    <tr id="<%= singledata.name %>tr">
                        <td scope="row" class="moviename" id="<%= singledata.name %>"><%= singledata.name %></td>
                        <td><%= singledata.genre %></td>
                        <td><%= singledata.rating %></td>
                        <td><%= singledata.language %></td>
                        <td>
                            <div class="dropdown open">
                            <% if(singledata.achievements == undefined){%>
                            <button class="btn btn-danger dropdown-toggle" type="button" id="<%=singledata.name%>status" data-toggle="dropdown" aria-haspopup="true"
                                    aria-expanded="false">
                                        Select Superhit or Super duper hit
                            </button>
                            <% }else{ %>
                                <button for= "datafound" class="btn btn-primary dropdown-toggle" type="button" id="<%=singledata.name%>status" data-toggle="dropdown" aria-haspopup="true"
                                    aria-expanded="false">
                                       <%= singledata.achievements%>
                            </button>
                            <%}%>
                            <div class="dropdown-menu" aria-labelledby="triggerId">
                                <a class="dropdown-item" id="<%=singledata.name%>hit" href="#">SuperHit</a>
                                <a class="dropdown-item disabled"  id="<%=singledata.name%>duphit" href="#">Super DuperHit</a>
                            </div>
                        </div></td>
                    </tr>
                <% } )%>
               
            <%}%>

        </tbody>
</table>
<script type="text/javascript" src="../js/achievement.js"></script>
<!-- <script type="text/javascript" src=""></script> -->
</body>
</html>